### Run the Application
To start the application, use the following command in your terminal:

```bash
node app.js
```

### Run Performance Test
To execute the automation tests, paste the following command in your terminal:

```bash
jmeter -n -t .\performance-testing.jmx -l results.jtl -e -o report
```